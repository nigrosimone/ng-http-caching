# ng-http-caching example

Minimal Angular app using `ng-http-caching` from npm. Run it in the browser:
**https://stackblitz.com/github/nigrosimone/ng-http-caching/tree/master/examples/stackblitz**

Locally: `npm install && npm start`.
